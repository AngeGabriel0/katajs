'use strict'
function min(a, b) {
  //Math.min renvoie le plus petit nombre d'une liste d'arguments
  Math.min(a, b);
  return Math.min(a, b);
}

console.log(min(34, 56)); //la console retourne 34, le plus petit nombre
